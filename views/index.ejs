<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <title>All posts</title>
</head>
<body>
    <div class="container">
        <h1>Inquira</h1>

        <% for (let post of posts) { %>
        <div class="post">
            <h3 class="user">@ <%= post.username %></h3>  
            <p class="content"><%= post.content %></p>
            <br>
            <a href="https://inquira-1.onrender.com/posts/<%= post.id%>">View details</a>
            <a href="https://inquira-1.onrender.com/posts/<%= post.id%>/edit">Edit details</a>
            <form action="/posts/<%= post.id%>?_method=DELETE" method="post">
                <button>Delete Post</button>
            </form>
        </div>
        <hr>
        <% } %> 

        <br>
        <br>

        <div style="text-align: center; background: none; border: none; box-shadow: none; padding: 0;">
            <a href="https://inquira-1.onrender.com/posts/new">Create post</a>
        </div>
    </div>
</body>
</html>